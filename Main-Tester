local percent = 0

local function printLoadingText()
    local randomUNC = math.random(49, 70)
    local randomStability = math.random(69, 80)
    local randomBypass = math.random(34, 50)
    
    while percent < 100 do
        wait(0.5)
        percent = percent + 1
        print("Testing UNC support -- Loading " .. percent .. "%")
    end
    print("UNC TEST SUCCESSFUL, RESULTS:")
    print("⏫️ UNC Support = " .. randomUNC .. "%")
    print("〰️ Stability Test = " .. randomStability .. "%")
    print("🔛 Successfully Bypassed Anti-cheats = " .. randomBypass .. "%")
end

printLoadingText()
